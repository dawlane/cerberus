configs
The scripts here are the default Cerberus config files in case the others get deleted. 
You can pass parameters to change a number of thrid party tool paths.
Open the files to see what the parameters are.
The xcode_icon_scrip is for internal xcode use.

macos_install
Installation files for use with creating Apple flat pack (pkg) files.